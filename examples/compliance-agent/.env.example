# Tempo MCP Compliance Agent Configuration
# Copy this to .env and fill in your values

# Required: Your wallet private key (with 0x prefix)
# This wallet should have policy owner permissions for compliance management
TEMPO_PRIVATE_KEY=0x...

# Optional: Default policy ID for compliance operations
TEMPO_DEFAULT_POLICY_ID=1

# Optional: Override RPC URL (defaults to Tempo testnet)
# TEMPO_RPC_URL=https://rpc.testnet.tempo.xyz

# Optional: Override Chain ID (defaults to 42429)
# TEMPO_CHAIN_ID=42429
